/**
 * Screen Brightness and nightlight
 */
import ScreenMask from '@/window-screen-mask/ScreenMask.vue'
import { createApp } from 'vue'

export const windowScreenMaskInstance = createApp(ScreenMask)
